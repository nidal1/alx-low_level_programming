#include <stdio.h>
#include "main.h"

/**
 * add - adds two integrers and returns the result
 *
 * @a: number being added
 *
 * @b: number being added
 *
 * Return: integer
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
